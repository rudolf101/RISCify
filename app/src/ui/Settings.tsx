import { ReactNode } from "react";

export type SettingsBoxProps = {
    // TODO: may add more
};

export function SettingsBox(props: SettingsBoxProps): ReactNode {
    return <></>;
    // TODO
    /*
    Рендерит интпуты для настроек дизассемблера:
    - переключатель 32 / 64 бит
    - (пока только это)
    */
}
